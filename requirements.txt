# ============================================================
# 城市扩张遥感分析系统 v2.0 — Python 依赖
# ============================================================
# 安装：pip install -r requirements.txt
# ============================================================

# --- Google Earth Engine Python API ---
# GEE 云端遥感数据存取 + 计算引擎
earthengine-api>=0.1.400

# --- geemap — GEE 可视化库 ---
# 交互式地图 + 影像下载 + 图层管理
geemap>=0.32.0

# --- 数值计算 ---
numpy>=1.24.0

# --- 数据分析 + CSV 导出 ---
pandas>=1.5.0

# --- 可视化（静态图表）---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- 图像处理（GIF 动图合成）---
Pillow>=9.0.0

# --- Jupyter Notebook 支持 ---
jupyter>=1.0.0
ipywidgets>=8.0.0

# --- 可选：本地栅格处理（当不用 GEE 时）---
rasterio>=1.3.0
